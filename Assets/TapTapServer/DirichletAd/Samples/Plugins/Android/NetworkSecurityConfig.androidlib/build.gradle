apply plugin: 'com.android.library'

android {
    namespace "com.dirichlet.samples.networksecurity"
    compileSdkVersion 34
    buildToolsVersion '34.0.0'

    defaultConfig {
        minSdkVersion 21
    }

    sourceSets {
        main {
            res.srcDirs = ['res']
        }
    }
}
